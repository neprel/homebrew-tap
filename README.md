# Neprel Homebrew tap

Homebrew formulae for public command-line tools maintained by Neprel.

```sh
brew tap neprel/tap
```

## Formulae

| Formula | Install |
| --- | --- |
| `git-a2a` | `brew install neprel/tap/git-a2a` |

More formulae may be added without creating a separate tap for each project.
